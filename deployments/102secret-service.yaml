apiVersion: v1
kind: Service
metatdata:
         name:
             app: secret-service
spec:
    selector:
            app: secret-service
    ports:
        - protocol: TCP
          port: 3000
          targetPort: 3000
    type: ClusterIP
