apiVersion: v1
kind: Service
metadata:
         name:
             app: email-service
spec:
    selector:
            app: email-service
    ports:
        - protocol: TCP
          port: 3000
          targetPort: 3000
    type: ClusterIP
