apiVersion: v1
kind: Service
metatdata:
         name:
             app: aes-encyption-service
spec:
    selector:
            app: aes-encyption-service
    ports:
        - protocol: TCP
          port: 3000
          targetPort: 3000
    type: ClusterIP
